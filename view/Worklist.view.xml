<mvc:View
    controllerName="project1.project1.controller.Worklist"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:semantic="sap.f.semantic"
>
    <Page showHeader="false">
        <Table
            id="table"
            width="auto"

            growing="true"
            growingThreshold="15"
            growingScrollToLoad="true"
        >
            <headerToolbar>
                <Toolbar>
                    <Title text="{i18n>tableTitle}" />
                    <Title id="idTableHeader" text="{=${worklistView>/count}}"/>
                    <ToolbarSpacer />
                    <SearchField
                        placeholder="{i18n>searchPlaceholder}"
                        liveChange="onSearchDocNumber"
                        width="auto"
                    />
                    <SearchField
                        placeholder="{i18n>plantSearchPlaceholder}"
                        liveChange="onSearchPlantText"
                        width="auto"
                    />
                </Toolbar>
            </headerToolbar>
            <columns>
                <Column>
                    <Text text="{i18n>documentNumber}" />
                </Column>
                <Column>
                    <Text text="{i18n>documentDate}" />
                </Column>
                <Column>
                    <Text text="{i18n>plantText}" />
                </Column>
                <Column>
                    <Text text="{i18n>regionText}" />
                </Column>
                <Column>
                    <Text text="{i18n>description}" />
                </Column>
                <Column>
                    <Text text="{i18n>created}" />
                </Column>
            </columns>
      
        </Table>
    </Page>
</mvc:View>
